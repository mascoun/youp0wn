<%= form_for(challenge) do |f| %>
  <% if challenge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(challenge.errors.count, "error") %> prohibited this challenge from being saved:</h2>

      <ul>
      <% challenge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :flag %>
    <%= f.text_field :flag %>
  </div>

  <div class="field">
    <%= f.label :attachement %>
    <%= f.text_field :attachement %>
  </div>

  <div class="field">
    <%= f.label :contest %>
    <%= f.select :contest_id, options_from_collection_for_select(Contest.all, :id, :name) %>
  </div>

  <div class="field">
    <%= f.label :category %>
    <%= f.select :category_id, options_from_collection_for_select(Category.all, :id, :name) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
