<header id="header">
         <nav class="navbar navbar-default navbar-fixed-top">
            <div class="container">
               <div class="navbar-header">
                  <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                  <span class="sr-only">Toggle navigation</span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  </button>
                <%=  image_tag('Logo-yp.png' ,height: "48" , width: "48", alt: "Logo You P0WN")    %>
                <span class="navbar-brand navbar-text hidden">You P0WN</span>
               </div>
               <div class="collapse navbar-collapse">
                  <ul class="nav navbar-nav">
                    <li><%= link_to "Home", root_url   %></li>
                    <li><%= link_to "Contests", contests_url   %></li>
                    <% if logged_in? and current_user.superuser? %>
                    <li><%= link_to "Challenges", challenges_url   %></li>
                    <% end %>
                  </ul>
                  <ul class="nav navbar-nav pull-right">
                  <% if logged_in? %>
                    <li><%= link_to "Logout", logout_url ,:method => :delete  %></li>
                  <% else %>
                    <li><%= link_to "Login", login_url   %></li>
                  <% end %>
                  </ul>
               </div>
            </div>
         </nav>
      </header>

